import tensorflow as tf # type: ignore
from tensorflow.keras.processing.image import Image # type: ignore
from tensorflow.keras import layers, models # type: ignore

def training_model():
    return


if __name__ == "__main__":
    training_model() # calling the method
